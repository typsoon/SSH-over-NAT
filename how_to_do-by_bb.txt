Rendezvous server:
    ps -a | grep rendezvous.py
    // checks if the server is running - kill it if true
    sudo python3 rendezvous.py
    // run the server

Server (where you want to connect to):
    // run the thing:
    python3 poc.py <username> <external_server_ip> <external_server_port> <arbitrary_unused_port> server
        // example:
        python3 poc.py riper 13.61.13.117 50 7777 server


Client (where you're connecting from):
        // before anything you need to check for "PROBLEM WITH TARGET" (hardcoded username of server)
    // run the thing:
    python3 poc.py <username> <external_server_ip> <external_server_port> <arbitrary_unused_port> client
        // example:
        python3 poc.py bb 13.61.13.117 50 8888 client





Potential problems:
    firewall on the side of server shall not be strict (look up: "symmetric nat") (hotspots don't work)


